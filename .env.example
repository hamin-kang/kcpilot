# PostgreSQL 접속 정보 — docker-compose가 이 값으로 컨테이너를 생성한다.
# USER/PASSWORD는 backend의 application.yaml datasource와 반드시 일치해야 한다.
POSTGRES_DB=kcpilot
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password